{
  "$comment": "Known-failing tests, by name. Generated by `node tools/test-ratchet.mjs --update` -- do not hand-edit the list. Lowering this file is the visible, reviewable act of accepting that a failure was fixed; raising it is the visible act of accepting a new one. Notes are preserved across --update, so keep them.",
  "command": "npm test",
  "knownFailures": [
    {
      "name": "agents generator emits exact canonical control targets for every truthful presence status",
      "note": ""
    },
    {
      "name": "browser reader accepts all checked-in available projections",
      "note": "Sits on the checked-in public/data/*.json fixtures; see the research-view rows below."
    },
    {
      "name": "contract #7: a real listen error that is not \"address in use\" propagates unchanged",
      "note": "The EACCES question, already with an independent adjudicator. Expect this NAME to change as that lane edits the contract suite; a rename reads here as one regression plus one improvement, which is correct, and means re-run --update rather than argue with the gate."
    },
    {
      "name": "discovery walks 4610-4619 in order and authenticates only after an exact valid tuple",
      "note": "ENVIRONMENT-COUPLED. Walks a real port range, and there is a live listener on 4610 on this host (scheduled task 'ToolsEnabled Mission Bridge', pid 9428). It can flip with no commit behind it: on a clean machine, or when that bridge stops, or when it restarts onto the newer proof-required build. If this appears fixed, confirm bridge state before lowering the baseline."
    },
    {
      "name": "research view distinguishes unavailable observations from observed-empty data",
      "note": "Sits on the checked-in public/data/*.json fixtures. The open question of whether those owner-private files should ship at all will move this test; coordinate with the data lane before lowering."
    },
    {
      "name": "research view keeps authorization-gated rows metadata-only",
      "note": "Sits on the checked-in public/data/*.json fixtures. Same coupling as the row above."
    }
  ],
  "updated": "2026-08-09T05:07:33.121Z"
}
