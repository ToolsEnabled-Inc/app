{
  "$comment": [
    "SHIPPED DEFAULT managed-process registry for a fresh installation.",
    "",
    "src/lib/managed-processes.js loadRegistry() fails closed: a missing file",
    "throws, and so does a registry that declares zero processes. That throw is",
    "not lazy -- src/lib/owner-steps.js resolves four taskNames out of this",
    "registry in a module-level const, so on a machine without this file the",
    "require() of owner-steps.js throws, and with it owner-chat.js and",
    "telegram-bridge-commands.js. Measured in a sterile payload run:",
    "'managed-processes: registry file unreadable at <root>/config/managed-",
    "processes.json'.",
    "",
    "The builder's own registry cannot be the file that ships. It describes the",
    "builder's fleet -- twenty-one processes, a sibling checkout of another repo",
    "at ../AgentActivityVisualizer, the two named Windows accounts the owner-host",
    "entry runs its server and client halves as, and a real Google Cloud project",
    "id in the fleet-supervisor argv. None of that is true on a customer's",
    "machine, and the account names and project id are personal data. They are",
    "described here rather than quoted, because a file that explains an owner-",
    "data leak by reproducing the owner data is still an owner-data leak -- the",
    "packer's guard caught exactly that in the first draft of this comment.",
    "",
    "So this file declares exactly the four processes the shipped payload can",
    "actually reach, and declares them at product level only: what each one is",
    "called, what it is, and where its entry point lives inside this payload.",
    "declaredArgv is empty everywhere, because a fresh installation has",
    "registered no scheduled tasks and inventing an argv -- particularly the",
    "fleet-supervisor --project value, which is a real cloud project id -- would",
    "be the same category of lie as shipping the builder's. Setup writes over",
    "this once the customer registers anything.",
    "",
    "The owner-host entry is deliberately absent rather than neutralised: its",
    "validation requires two DIFFERENT real Windows principals, so there is no",
    "honest placeholder for it, and nothing in the shipped payload reads it."
  ],
  "schemaVersion": 1,
  "processes": {
    "dashboard": {
      "displayName": "Agent Activity Dashboard",
      "taskName": "ToolsEnabled Dashboard",
      "kind": "listener",
      "owner": "dashboard-agents",
      "repo": "ToolsEnabled",
      "entryPoint": "",
      "entryPattern": "",
      "declaredArgv": [],
      "cwd": ".",
      "port": null,
      "registrar": null
    },
    "jarvis": {
      "displayName": "JARVIS local-coder sidecar",
      "taskName": "ToolsEnabled JARVIS Server",
      "kind": "listener",
      "owner": "jarvis",
      "repo": "ToolsEnabled",
      "entryPoint": "sidecars/local-coder/bin/server.js",
      "entryPattern": "sidecars/local-coder/bin/server.js",
      "declaredArgv": [],
      "cwd": ".",
      "port": null,
      "registrar": null
    },
    "uac-delegation-helper": {
      "displayName": "UAC Delegation Helper",
      "taskName": "ToolsEnabled UAC Delegation Helper",
      "kind": "on-demand",
      "owner": "uac-delegation",
      "repo": "ToolsEnabled",
      "entryPoint": "src/uac-delegation-helper.js",
      "entryPattern": "src/uac-delegation-helper.js",
      "declaredArgv": [],
      "cwd": ".",
      "port": null,
      "onDemand": true,
      "registrar": null
    },
    "fleet-supervisor": {
      "displayName": "Gemini Fleet Supervisor",
      "taskName": "ToolsEnabled Fleet Supervisor",
      "kind": "worker",
      "owner": "fleet-supervisor",
      "repo": "ToolsEnabled",
      "entryPoint": "tools/fleet-supervisor.js",
      "entryPattern": "tools/fleet-supervisor.js",
      "declaredArgv": [],
      "cwd": ".",
      "port": null,
      "registrar": null
    }
  }
}
